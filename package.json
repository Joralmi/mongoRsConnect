{
  "name": "testconnection",
  "version": "1.0.0",
  "description": "Node JS, Mongo, replica set, SSL, self signed cert, X.509",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongoose": "^5.5.0"
  }
}
